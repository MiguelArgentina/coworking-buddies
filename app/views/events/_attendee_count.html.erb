<div id="attendee_count_event_<%= event.id %>">
  <span id="attendee_count_event_<%= event.id %>" class="text-sm text-gray-600">
    👥 <%= event.event_participations.count %> attending
  </span>
</div>